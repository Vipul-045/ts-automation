import { Signer } from "@ethersproject/abstract-signer";
import { ChainId } from "../types";
export declare class Signature {
    private readonly _timeTillExpiration;
    private readonly _expirationBuffer;
    private readonly _signer;
    private readonly _chainId;
    private readonly _signatureMessage;
    private readonly _signatureDomain;
    private _lastExpirationTime;
    private _token;
    constructor(chainId: ChainId, signer: Signer, signatureMessage?: string, signatureDomain?: string);
    getAuthToken(): Promise<string>;
    private _requestAndStoreSignature;
}
