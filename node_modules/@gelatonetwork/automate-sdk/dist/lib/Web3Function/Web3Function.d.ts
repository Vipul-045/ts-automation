import { Signer } from "@ethersproject/abstract-signer";
import { ChainId, Config } from "../../types";
import { Web3FunctionSecrets } from "./Web3FunctionSecrets";
import { Web3FunctionStorage } from "./Web3FunctionStorage";
export declare class Web3Function {
    secrets: Web3FunctionSecrets;
    storage: Web3FunctionStorage;
    private _signature;
    constructor(chainId: ChainId, signer: Signer, signatureMessage?: string, config?: Partial<Config>);
}
