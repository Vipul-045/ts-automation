import { Signer } from "@ethersproject/abstract-signer";
import { Config, Secrets } from "../../types";
import { Signature } from "../Signature";
export declare class Web3FunctionSecrets {
    private readonly _signer;
    private readonly _userApi;
    private _signature;
    private _chainId?;
    constructor(signer: Signer, signature: Signature, config?: Partial<Config>);
    get(key: string, taskId?: string, authToken?: string, address?: string): Promise<string>;
    list(taskId?: string, authToken?: string, address?: string): Promise<Secrets>;
    set(secrets: Secrets, taskId?: string, authToken?: string, address?: string): Promise<void>;
    delete(key: string, taskId?: string, authToken?: string, address?: string): Promise<void>;
    private _initialize;
}
