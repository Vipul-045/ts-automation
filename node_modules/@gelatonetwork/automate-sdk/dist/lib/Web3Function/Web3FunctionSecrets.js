"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Web3FunctionSecrets = void 0;
const axios_1 = __importDefault(require("axios"));
const constants_1 = require("../../constants");
const utils_1 = require("../../utils");
class Web3FunctionSecrets {
    constructor(signer, signature, config) {
        var _a;
        this._signer = signer;
        let userApiUrl = constants_1.AUTOMATE_USER_API;
        if (config) {
            userApiUrl =
                ((_a = config.userApi) !== null && _a !== void 0 ? _a : config.isDevelopment)
                    ? constants_1.AUTOMATE_USER_DEV_API
                    : constants_1.AUTOMATE_USER_API;
        }
        this._userApi = axios_1.default.create({
            baseURL: userApiUrl,
        });
        this._signature = signature;
    }
    get(key, taskId, authToken, address) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                yield this._initialize();
                if (!address)
                    address = yield this._signer.getAddress();
                if (!authToken)
                    authToken = yield this._signature.getAuthToken();
                const route = taskId
                    ? `/users/${address}/secrets/${key}/${this._chainId}/${taskId}`
                    : `/users/${address}/secrets/${key}`;
                const res = yield this._userApi.get(route, {
                    headers: { Authorization: `Bearer ${authToken}` },
                });
                const secret = res.data[key];
                return secret;
            }
            catch (err) {
                const errMsg = (0, utils_1.errorMessage)(err);
                throw new Error(`Fail to get secret for key "${key}". \n${errMsg}`);
            }
        });
    }
    list(taskId, authToken, address) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                yield this._initialize();
                if (!address)
                    address = yield this._signer.getAddress();
                if (!authToken)
                    authToken = yield this._signature.getAuthToken();
                const route = taskId
                    ? `/users/${address}/secrets/${this._chainId}/${taskId}`
                    : `/users/${address}/secrets`;
                const res = yield this._userApi.get(route, {
                    headers: { Authorization: `Bearer ${authToken}` },
                });
                return res.data;
            }
            catch (err) {
                const errMsg = (0, utils_1.errorMessage)(err);
                throw new Error(`Fail to list all secrets. \n${errMsg}`);
            }
        });
    }
    set(secrets, taskId, authToken, address) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                yield this._initialize();
                if (!address)
                    address = yield this._signer.getAddress();
                if (!authToken)
                    authToken = yield this._signature.getAuthToken();
                const route = taskId
                    ? `/users/${address}/secrets/${this._chainId}/${taskId}`
                    : `/users/${address}/secrets`;
                yield this._userApi.post(route, Object.assign({}, secrets), {
                    headers: { Authorization: `Bearer ${authToken}` },
                });
            }
            catch (err) {
                const errMsg = (0, utils_1.errorMessage)(err);
                throw new Error(`Fail to set secrets ${secrets}. \n${errMsg}`);
            }
        });
    }
    delete(key, taskId, authToken, address) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                yield this._initialize();
                if (!address)
                    address = yield this._signer.getAddress();
                if (!authToken)
                    authToken = yield this._signature.getAuthToken();
                const route = taskId
                    ? `/users/${address}/secrets/${key}/${this._chainId}/${taskId}`
                    : `/users/${address}/secrets/${key}`;
                yield this._userApi.delete(route, {
                    headers: { Authorization: `Bearer ${authToken}` },
                });
            }
            catch (err) {
                const errMsg = (0, utils_1.errorMessage)(err);
                throw new Error(`Fail to delete secret "${key}". \n${errMsg}`);
            }
        });
    }
    _initialize() {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this._chainId) {
                this._chainId = yield this._signer.getChainId();
            }
        });
    }
}
exports.Web3FunctionSecrets = Web3FunctionSecrets;
