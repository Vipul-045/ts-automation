import { Signer } from "@ethersproject/abstract-signer";
import { ChainId, Config, Storage } from "../../types";
import { Signature } from "../Signature";
export declare class Web3FunctionStorage {
    private readonly _signer;
    private readonly _userApi;
    private _signature;
    constructor(signer: Signer, signature: Signature, config?: Partial<Config>);
    get(chainId: ChainId, taskId: string, authToken?: string): Promise<Storage>;
}
